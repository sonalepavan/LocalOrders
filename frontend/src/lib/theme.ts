import { MD3LightTheme, MD3DarkTheme } from "react-native-paper";

// Clean default Material 3 themes (light + dark). No custom palette tweaks.
export const lightTheme = {
  ...MD3LightTheme,
};

export const darkTheme = {
  ...MD3DarkTheme,
};
